default: &default
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see rails configuration guide
  # http://guides.rubyonrails.org/configuring.html#database-pooling
  pool: 50

development:
  <<: *default
  host: localhost
  database: zinger_development
  username: postgres
  password: postgres
